<div class="row">
    <div class="col-md-6 col-md-offset-3 well">
        <% @game.users.each_with_index do |u, index| %>
        
        <%= index.to_s + " - " + u.name %>
        
        <% end %>
    </div>
</div>